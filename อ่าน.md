# JS ที่ทำงานในหน้า main.html

## ภาพรวม

เมื่อเปิดหน้า `main.html` ไฟล์ `app.js` จะทำงานตามลำดับดังนี้ โดยหน้า main เป็น **landing page คงที่** — JS ทำหน้าที่เตรียมข้อมูลใน state เท่านั้น (เผื่อหน้าอื่นจะใช้ต่อ) ไม่มีการ render หรือจัดการ UI แบบ dynamic

---

## ลำดับการทำงาน

```
DOMContentLoaded
  → init()
      → loadData()
          → localStorage ว่าง?
              → seedBookings() → สร้าง booking ตัวอย่าง
              → seedQueue()   → สร้างคิวตัวอย่าง
              → saveData()    → เขียนลง localStorage
      → loadUserSession()     → โหลด session ผู้ใช้ (ถ้ามี)
      → detectCurrentPage()   → ได้ 'home'
      → nav toggle setup      → ปุ่ม hamburger (มือถือ)
      → ข้ามทุก page-specific block (ไม่มี if home)
```

---

## รายละเอียดแต่ละชุดโค้ด (อธิบายทีละบรรทัด)

### 1. `CONFIG` (บรรทัด 6–24) — ตั้งค่าคงที่ของระบบ

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 6 | `const CONFIG = {` | ประกาศตัวแปรคงที่เก็บการตั้งค่าทั้งหมด (const = เขียนทับไม่ได้) |
| 7 | `courts: 12,` | จำนวนสนาม 12 คอร์ท (ใช้วาดตาราง booking) |
| 8 | `openHour: 10,` | เปิด 10:00 (ใช้คำนวณช่วงชั่วโมงในตาราง) |
| 9 | `closeHour: 24,` | ปิด 00:00 (ช่วงสุดท้ายในตาราง) |
| 10-14 | `pricing: (hour) => {...}` | ฟังก์ชันคำนวณราคา: 10-15→฿150, 16-20→฿250, อื่นๆ→฿200 |
| 15 | `adminPassword: 'admin1234',` | รหัสผ่านเข้าหน้าแอดมิน |
| 16 | `storageKey: 'shuttle_club_v2',` | ชื่อ key localStorage (ใช้ใน loadData/saveData) |
| 17 | `matchDuration: 15 * 60,` | ระยะเวลาแมตช์ 15 นาที = 900 วินาที |
| 18-23 | `skillLevels: {...}` | 4 ระดับฝีมือ BG/M/P/S พร้อม name+desc สำหรับ matchmaking |

---

### 2. `state` (บรรทัด 27–39) — สร้าง object เก็บสถานะปัจจุบันของแอป

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 27 | `const state = {` | ประกาศตัวแปรกลางที่ทุกฟังก์ชันอ่าน/เขียนร่วมกัน |
| 28 | `selectedDate: null,` | วันที่ผู้ใช้เลือกในหน้า booking (null = ยังไม่เลือก) |
| 29 | `selectedSlots: [],` | array เก็บช่องเวลาที่เลือก `{ court, hour, price }` |
| 30 | `bookings: [],` | array เก็บข้อมูลการจองทั้งหมด (โหลดจาก localStorage หรือ seed) |
| 31 | `queue: [],` | array เก็บรายชื่อคนในคิวรอจับคู่ (หน้า matchmaking) |
| 32 | `matches: [],` | array เก็บแมตช์ที่กำลังเล่น/จบแล้ว |
| 33 | `currentPage: 'home',` | ชื่อหน้าปัจจุบัน (จะถูกเขียนทับด้วย detectCurrentPage()) |
| 34 | `admin: { loggedIn: false },` | สถานะล็อกอินแอดมิน (false = ยังไม่ล็อกอิน) |
| 35 | `user: { loggedIn: false, phone: null, name: null },` | สถานะผู้ใช้: loggedIn/phone/name |
| 36 | `mmForm: { level: null, mode: null },` | ค่าที่เลือกในฟอร์ม matchmaking (level=BG/M/P/S, mode=single/double) |
| 37 | `queueFilter: 'all',` | ตัวกรองคิว (ค่าเริ่มต้นแสดงทุกระดับ) |
| 38 | `selfQueueId: null,` | ID ของตัวผู้ใช้เองในคิว (ถ้ามี) เพื่อไฮไลท์การ์ดตัวเอง |

---

### 3. `loadData()` (บรรทัด 42–60) — โหลดข้อมูลจาก localStorage

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 42 | `function loadData() {` | ประกาศฟังก์ชัน ไม่รับพารามิเตอร์ แก้ไข state โดยตรง |
| 43 | `try {` | เริ่ม try-catch เพื่อจับ error (localStorage เสีย/JSON ผิดรูป) |
| 44 | `const raw = localStorage.getItem(CONFIG.storageKey);` | อ่านข้อมูลจาก localStorage key `'shuttle_club_v2'` → string หรือ null |
| 45 | `if (raw) {` | ตรวจว่ามีข้อมูลอยู่แล้ว (raw ไม่ใช่ null) |
| 46 | `const data = JSON.parse(raw);` | แปลง JSON string เป็น JavaScript object |
| 47 | `state.bookings = data.bookings \|\| [];` | เอา bookings จาก data ใส่ state (ถ้าไม่มีใช้ array ว่าง) |
| 48 | `state.queue = data.queue \|\| [];` | เอา queue จาก data ใส่ state |
| 49 | `state.matches = data.matches \|\| [];` | เอา matches จาก data ใส่ state |
| 50 | `} else {` | กรณี raw เป็น null = ไม่เคยมีข้อมูล (เปิดครั้งแรก) |
| 51 | `state.bookings = seedBookings();` | เรียก seedBookings() สร้างข้อมูลจองตัวอย่าง 6 รายการ |
| 52 | `state.queue = seedQueue();` | เรียก seedQueue() สร้างข้อมูลคิวตัวอย่าง 5 คน |
| 53 | `state.matches = [];` | ตั้ง matches เป็น array ว่าง (ยังไม่มีแมตช์) |
| 54 | `saveData();` | บันทึกข้อมูลตัวอย่างลง localStorage เผื่อครั้งต่อไป |
| 56 | `} catch {` | ทำงานเมื่อเกิด error ใน try |
| 57 | `state.bookings = [];` | ตั้ง bookings เป็น array ว่าง (กรณี error) |
| 58 | `state.queue = [];` | ตั้ง queue เป็น array ว่าง |
| 59 | `state.matches = [];` | ตั้ง matches เป็น array ว่าง |

---

### 4. `saveData()` (บรรทัด 62–74) — บันทึก state ลง localStorage

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 62 | `function saveData() {` | ประกาศฟังก์ชัน ไม่รับพารามิเตอร์ |
| 63-67 | `localStorage.setItem(CONFIG.storageKey, JSON.stringify({bookings, queue, matches}))` | แปลง state เป็น JSON string แล้วเขียนลง localStorage key `'shuttle_club_v2'` — ข้อมูลคงอยู่แม้ปิดเบราว์เซอร์ |
| 69 | `if (state.user.loggedIn) {` | ตรวจว่าผู้ใช้ล็อกอินอยู่หรือไม่ |
| 70 | `localStorage.setItem('shuttle_club_user', JSON.stringify(state.user))` | ถ้าล็อกอิน → บันทึกข้อมูลผู้ใช้ (loggedIn, phone, name) ลง key แยก |
| 72 | `localStorage.removeItem('shuttle_club_user')` | ถ้าไม่ล็อกอิน → ลบ session เก่าทิ้ง |

---

### 5. `loadUserSession()` (บรรทัด 76–86) — โหลด session ผู้ใช้จาก localStorage

โหลด session ผู้ใช้จาก localStorage

- อ่าน `localStorage.getItem('shuttle_club_user')`
- ถ้ามีและ `data.loggedIn === true` และมี `data.phone` → เอาข้อมูลมาใส่ `state.user`
- ถ้าไม่มี → `state.user` ยังเป็น `{ loggedIn: false, phone: null, name: null }`
- หน้า main ไม่ได้แสดงข้อมูลผู้ใช้ แต่การโหลด session นี้สำคัญเพื่อให้ **เมื่อไปหน้าอื่น** จะยังจำผู้ใช้อยู่

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 76 | `function loadUserSession() {` | ประกาศฟังก์ชัน ไม่รับพารามิเตอร์ |
| 77 | `try {` | เริ่ม try-catch เพื่อจับ error |
| 78 | `const raw = localStorage.getItem('shuttle_club_user');` | อ่านข้อมูลผู้ใช้จาก localStorage key `'shuttle_club_user'` |
| 79 | `if (raw) {` | ตรวจว่ามีข้อมูลหรือไม่ |
| 80 | `const data = JSON.parse(raw);` | แปลง JSON string เป็น object |
| 81 | `if (data.loggedIn && data.phone) {` | ตรวจสอบ 2 เงื่อนไข: `loggedIn` เป็น true และมี `phone` |
| 82 | `state.user = data;` | ถ้าเงื่อนไขเป็นจริง → เขียนทับ `state.user` ด้วยข้อมูลจาก localStorage |
| 85 | `} catch { }` | ถ้าเกิด error (เช่น JSON เสีย) ก็ไม่ทำอะไร ปล่อยให้ state.user เป็นค่าเริ่มต้น |

> **หมายเหตุ**: หน้า main ไม่ได้แสดงข้อมูลผู้ใช้ แต่การโหลด session นี้สำคัญเพื่อให้เมื่อไปหน้าอื่นจะยังจำผู้ใช้อยู่

---

### 6. `seedBookings()` (บรรทัด 88–114) — สร้างข้อมูลจองตัวอย่าง 6 รายการ (ทำงานเฉพาะเมื่อ localStorage ว่าง)

สร้างข้อมูลจองตัวอย่าง 6 รายการ (ทำงานเฉพาะเมื่อ localStorage ว่าง)

- วนลูป array `ex` ที่มีข้อมูลชื่อ/เบอร์/คอร์ท/ชั่วโมง
- คำนวณวันที่จากวันนี้ + offset (0, 1, 2 วัน)
- สร้าง object booking พร้อม:
  - `id` — เช่น "BK1234567"
  - `date` — เช่น "2026-04-22"
  - `slots` — แปลง c→court, h→hour และเรียก `CONFIG.pricing()` หาราคา
  - `total` — ราคารวม
  - `name`, `phone`, `note`, `status`, `createdAt`

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 88 | `function seedBookings() {` | ประกาศฟังก์ชัน คืนค่าเป็น array ของ booking objects |
| 89 | `const today = new Date();` | สร้าง Date object ของวันนี้ |
| 90 | `const fmt = (d) => d.toISOString().slice(0, 10);` | ฟังก์ชันย่อยแปลง Date → "YYYY-MM-DD" (เช่น "2026-04-22") |
| 91 | `const demo = [];` | สร้าง array ว่างเก็บผลลัพธ์ |
| 92-99 | `const ex = [...]` | กำหนดข้อมูลตัวอย่าง 6 รายการ: `d`=วัน offset, `slots`=[{c=คอร์ท, h=ชั่วโมง}], name, phone, status |
| 100 | `ex.forEach((e, i) => {` | วนลูปทุกรายการ `e`=รายการ, `i`=index (0-5) |
| 101 | `const date = new Date(today);` | คัดลอก Date ของวันนี้ (ต้องสร้างใหม่เพราะ setDate แก้ object เดิม) |
| 102 | `date.setDate(date.getDate() + e.d);` | บวกวัน offset: d=0→วันนี้, d=1→พรุ่งนี้, d=2→มะรืน |
| 103 | `demo.push({` | เพิ่ม booking object เข้า array demo |
| 104 | `id: 'BK' + String(Date.now() + i).slice(-7),` | สร้าง ID: timestamp + i → เอา 7 ตัวสุดท้าย → "BK8000001" |
| 105 | `date: fmt(date),` | วันที่ในรูปแบบ "2026-04-22" (ใช้ฟังก์ชัน fmt) |
| 106 | `slots: e.slots.map(s => ({ court: s.c, hour: s.h, price: CONFIG.pricing(s.h) }))` | แปลง slots: c→court, h→hour, คำนวณ price จาก CONFIG.pricing() |
| 107 | `total: e.slots.reduce((sum, s) => sum + CONFIG.pricing(s.h), 0),` | คำนวณราคารวม: สะสมราคาทุก slot (เช่น 250+250=500) |
| 108 | `name: e.name, phone: e.phone, note: '',` | คัดลอกชื่อ/เบอร์, note เป็น string ว่าง |
| 109 | `status: e.status,` | สถานะการจอง = 'confirmed' |
| 110 | `createdAt: Date.now() - (i * 3600000),` | เวลาสร้าง (จำลอง): ลบ i ชั่วโมง (3600000 ms = 1 ชม.) |
| 113 | `return demo;` | คืนค่า array ที่มี booking ทั้ง 6 รายการ |

ข้อมูลตัวอย่างที่สร้าง:

| ชื่อ | เบอร์ | คอร์ท | เวลา | วัน |
|---|---|---|---|---|
| ภาณุศักดิ์ ศรีสุวรรณ | 0812345678 | 1 | 18:00–20:00 | วันนี้ |
| ปรียา แสงจันทร์ | 0898765432 | 3 | 19:00–21:00 | วันนี้ |
| ธีรพงษ์ วงศ์ประเสริฐ | 0811111111 | 5 | 20:00–21:00 | วันนี้ |
| สุภาพร เจริญสุข | 0822222222 | 2 | 17:00–19:00 | พรุ่งนี้ |
| ชนาธิป บุญมาก | 0833333333 | 7 | 18:00–19:00 | พรุ่งนี้ |
| วรรณา สิริธรรม | 0844444444 | 4 | 19:00–22:00 | มะรืน |

---

### 7. `seedQueue()` (บรรทัด 116–125) — สร้างข้อมูลคิวตัวอย่าง 5 คน (ทำงานเฉพาะเมื่อ localStorage ว่าง)

สร้างข้อมูลคิวตัวอย่าง 5 คน (ทำงานเฉพาะเมื่อ localStorage ว่าง)

- แต่ละคนมี id, name, phone, level (BG/M/P), mode (single/double), joinedAt
- `joinedAt` คำนวณจาก `Date.now() - N นาที` เพื่อจำลองว่ารอมาแล้วกี่นาที

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 116 | `function seedQueue() {` | ประกาศฟังก์ชัน คืนค่าเป็น array ของ queue objects |
| 117 | `const now = Date.now();` | เก็บ timestamp ปัจจุบัน (มิลลิวินาที) เพื่อคำนวณเวลาเข้าคิว |
| 118-124 | `return [...]` | คืนค่า array 5 คน แต่ละคนมี: |
| — | `id: 'Q' + (now - 60000)` | ID เริ่มด้วย 'Q' + timestamp ที่ลดลง (ให้แต่ละคน ID ต่างกัน) |
| — | `name: 'พีรพัฒน์'` | ชื่อผู้เล่น |
| — | `phone: '0812223344'` | เบอร์โทร |
| — | `level: 'M'` | ระดับฝีมือ (BG/M/P) |
| — | `mode: 'single'` | โหมดเล่น ('single' = เดี่ยว, 'double' = คู่) |
| — | `joinedAt: now - 60000 * 5` | เวลาเข้าคิว: ลบออกเพื่อจำลองว่ารอมาแล้ว 5 นาที (60000 ms = 1 นาที) |

ข้อมูลตัวอย่างที่สร้าง:

| ชื่อ | เบอร์ | ระดับ | โหมด | รอมาแล้ว |
|---|---|---|---|---|
| พีรพัฒน์ | 0812223344 | M | single | 5 นาที |
| จิราภรณ์ | 0822334455 | M | single | 4 นาที |
| กิตติพงษ์ | 0833445566 | P | double | 3 นาที |
| สายฝน | 0844556677 | BG | double | 2 นาที |
| อนุชา | 0855667788 | P | double | 1 นาที |

---

### 8. Utility functions (บรรทัด 128–153) — ฟังก์ชันช่วยแปลงข้อมูล

ฟังก์ชันช่วยแปลงข้อมูล (ไม่ได้เรียกใช้โดยตรงบนหน้า main แต่ถูกเรียกจาก seedBookings/seedQueue)

- `fmtDate(dateStr)` — แปลงวันที่เป็นภาษาไทย เช่น "วันจันทร์ที่ 22 เม.ย. 2569"
- `fmtHour(h)` — แปลงตัวเลขชั่วโมงเป็น "18:00"
- `fmtSlot(hour)` — แปลงเป็นช่วงเวลา "18:00–19:00"
- `todayStr()` — ได้วันนี้ในรูปแบบ "2026-04-22"
- `genId(prefix)` — สร้าง ID เช่น "BK1234567"
- `fmtMMSS(sec)` — แปลงวินาทีเป็น "MM:SS"
- `fmtAgo(ms)` — แปลงเวลาที่ผ่านมาเป็น "5 นาที" / "2 ชม."
- `initials(name)` — ตัดชื่อเป็นตัวย่อ 2 ตัว เช่น "ภาณุศักดิ์ ศรีสุวรรณ" → "ภศ"

| บรรทัด | ฟังก์ชัน | โค้ดสำคัญ | หน้าที่ | ตัวอย่าง |
|---|---|---|---|---|
| 128-133 | `fmtDate(dateStr)` | `new Date(dateStr)` + `d.getDay()` + `d.getFullYear() + 543` | แปลง "2026-04-22" → "วันจันทร์ที่ 22 เม.ย. 2569" | `fmtDate('2026-04-22')` → "วันจันทร์ที่ 22 เม.ย. 2569" |
| 134 | `fmtHour(h)` | `String(h).padStart(2, '0') + ':00'` | แปลงตัวเลขชั่วโมงเป็น "HH:00" | `fmtHour(18)` → "18:00" |
| 135 | `fmtSlot(hour)` | `` `${fmtHour(hour)}–${fmtHour(hour+1)}` `` | แปลงเป็นช่วงเวลา | `fmtSlot(18)` → "18:00–19:00" |
| 136 | `todayStr()` | `new Date()` + ปี-เดือน-วัน | ได้วันนี้ในรูปแบบ "YYYY-MM-DD" | → "2026-04-22" |
| 137 | `genId(prefix)` | `prefix + String(Date.now()).slice(-7)` | สร้าง ID แบบ unique | `genId('BK')` → "BK8000123" |
| 138-142 | `fmtMMSS(sec)` | `Math.floor(sec/60)` + `sec%60` + padStart | แปลงวินาทีเป็น "MM:SS" | `fmtMMSS(125)` → "02:05" |
| 143-148 | `fmtAgo(ms)` | `(Date.now()-ms)/1000` → วิ/นาที/ชม. | แปลงเวลาที่ผ่านมาเป็นภาษาไทย | `fmtAgo(Date.now()-300000)` → "5 นาที" |
| 149-153 | `initials(name)` | `name.split(/\s+/)` → เอาตัวแรกของ 2 คำ | ตัดชื่อเป็นตัวย่อ 2 ตัว | `initials('ภาณุศักดิ์ ศรีสุวรรณ')` → "ภศ" |

---

### 9. `detectCurrentPage()` (บรรทัด 156–169) — ตรวจจับหน้าปัจจุบันจาก URL

ตรวจจับหน้าปัจจุบันจาก URL

- อ่าน `window.location.pathname` เช่น `/main.html`
- `.split('/').pop()` → ได้ `'main.html'`
- `.replace('.html', '')` → ได้ `'main'`
- ถ้าได้ string ว่าง (กรณี root URL) → ใช้ `'main'` เป็น default
- ค้นใน `pageMap` → `'main'` แมปเป็น `'home'`
- คืนค่า `'home'` ใส่ `state.currentPage`

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 156 | `function detectCurrentPage() {` | ประกาศฟังก์ชัน คืนค่าเป็นชื่อหน้า |
| 157 | `const path = window.location.pathname;` | อ่าน path จาก URL เช่น `/main.html` |
| 158 | `const file = path.split('/').pop().replace('.html', '') \|\| 'main';` | แตก path ด้วย '/' → เอาตัวสุดท้าย → ลบ '.html' → ถ้าว่างใช้ 'main' เป็น default |
| 159-167 | `const pageMap = {...}` | object แมปชื่อไฟล์ → ชื่อหน้าภายใน |
| 160 | `'main': 'home',` | main → home |
| 161 | `'index': 'home',` | index → home |
| 162 | `'booking': 'booking',` | booking → booking |
| 163 | `'matchmaking': 'matchmaking',` | matchmaking → matchmaking |
| 164 | `'history': 'history',` | history → history |
| 165 | `'user': 'user',` | user → user |
| 166 | `'admin': 'admin',` | admin → admin |
| 168 | `return pageMap[file] \|\| 'home';` | ค้นใน pageMap → ถ้าไม่เจอใช้ 'home' เป็นค่า default |

---

### 10. `init()` — ส่วนที่ทำงานบนหน้า main (บรรทัด 1466–1476)

จุดเริ่มต้นหลัก เรียกโดย `DOMContentLoaded` ที่บรรทัด 1600

- เรียก `loadData()` — โหลด/สร้างข้อมูล
- เรียก `loadUserSession()` — โหลด session ผู้ใช้
- ตั้ง `state.currentPage = detectCurrentPage()` → ได้ `'home'`
- ตั้ง mobile nav toggle (ปุ่ม hamburger)
- **ข้ามทุก `if (page === ...)` block** เพราะไม่มี `if (page === 'home')`

หน้าอื่นที่มี page-specific init แต่หน้า main ไม่มี:

- `if (page === 'booking')` → ตั้ง event listener ฟอร์มจอง, modal, payment
- `if (page === 'matchmaking')` → ตั้งฟอร์มคิว, interval timer
- `if (page === 'user')` → ตั้ง login/logout
- `if (page === 'admin')` → ตั้ง login, filter, table

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 1466 | `function init() {` | ประกาศฟังก์ชันหลักที่ทำงานเมื่อ DOM โหลดเสร็จ |
| 1467 | `loadData();` | เรียก loadData() → โหลด/สร้างข้อมูล bookings/queue/matches |
| 1468 | `loadUserSession();` | เรียก loadUserSession() → โหลด session ผู้ใช้จาก localStorage |
| 1469 | `state.currentPage = detectCurrentPage();` | ตรวจจับหน้าปัจจุบัน → ได้ `'home'` ใส่ state.currentPage |
| 1472 | `const navToggle = document.getElementById('navToggle');` | หาปุ่ม hamburger ใน DOM |
| 1473 | `const navLinks = document.querySelector('.nav-links');` | หาเมนูนำทางใน DOM |
| 1474 | `if (navToggle && navLinks) {` | ตรวจว่ามีทั้งสอง element (ป้องกัน error ถ้าหาไม่เจอ) |
| 1475 | `navToggle.onclick = () => navLinks.classList.toggle('open');` | ตั้ง onclick: คลิกปุ่ม → toggle class 'open' ของเมนู (แสดง/ซ่อน) |
| 1479 | `const page = state.currentPage;` | เก็บชื่อหน้าปัจจุบันในตัวแปร `page` = `'home'` |
| 1481+ | `if (page === 'booking') {...}` | **ข้าม** — page เป็น 'home' ไม่ตรงเงื่อนไข |
| 1537+ | `if (page === 'matchmaking') {...}` | **ข้าม** — page เป็น 'home' ไม่ตรงเงื่อนไข |
| 1554+ | `if (page === 'history') {...}` | **ข้าม** — page เป็น 'home' ไม่ตรงเงื่อนไข |
| 1567+ | `if (page === 'user') {...}` | **ข้าม** — page เป็น 'home' ไม่ตรงเงื่อนไข |
| 1576+ | `if (page === 'admin') {...}` | **ข้าม** — page เป็น 'home' ไม่ตรงเงื่อนไข |

> **สำคัญ**: ไม่มี `if (page === 'home')` block — หน้า main เป็นหน้าเดียวที่ไม่มี page-specific init

---

### 11. `window.*` exports (บรรทัด 1593–1598) — ผูกฟังก์ชันเข้า global scope

ผูกฟังก์ชันเข้า global scope เผื่อ inline `onclick` เรียกใช้

- `window.updateStatus`
- `window.leaveQueue`
- `window.finishMatch`
- `window.toggleBookingDetail`
- `window.toggleAdminDetail`
- `window.changeQty`

หน้า main ไม่มี inline onclick จึงไม่ถูกเรียกใช้

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 1593 | `window.updateStatus = updateStatus;` | ผูกฟังก์ชันเปลี่ยนสถานะ booking เข้า global (เรียกจาก inline onclick) |
| 1594 | `window.leaveQueue = leaveQueue;` | ผูกฟังก์ชันออกจากคิวเข้า global |
| 1595 | `window.finishMatch = finishMatch;` | ผูกฟังก์ชันจบแมตช์เข้า global |
| 1596 | `window.toggleBookingDetail = toggleBookingDetail;` | ผูกฟังก์ชันเปิด/ปิดรายละเอียด booking เข้า global |
| 1597 | `window.toggleAdminDetail = toggleAdminDetail;` | ผูกฟังก์ชันเปิด/ปิดรายละเอียด admin เข้า global |
| 1598 | `window.changeQty = changeQty;` | ผูกฟังก์ชันเปลี่ยนจำนวน addon เข้า global |

> **หมายเหตุ**: หน้า main ไม่มี inline onclick จึงไม่ได้เรียกใช้ฟังก์ชันเหล่านี้ แต่โค้ดนี้ทำงานทุกหน้าเพราะอยู่ใน app.js ส่วนร่วม

---

### 12. `DOMContentLoaded` (บรรทัด 1600) — จุดเริ่มต้นสุดท้าย

จุดเริ่มต้นสุดท้าย — เมื่อ DOM โหลดเสร็จจะเรียก `init()`

| บรรทัด | โค้ด | หน้าที่ |
|---|---|---|
| 1600 | `document.addEventListener('DOMContentLoaded', init);` | ลงทะเบียน event listener: เมื่อ HTML โหลดเสร็จทั้งหมด → เรียกฟังก์ชัน `init()` |

> `DOMContentLoaded` ทำงานเมื่อ DOM สร้างเสร็จ (ก่อนรูปภาพ/stylesheet โหลดเสร็จ) — ต่างจาก `window.onload` ที่รอทุกอย่างโหลดเสร็จ

---

## สรุป

หน้า main.html เป็นหน้าเดียวในระบบที่ **ไม่มี render function หรือ event listener เฉพาะ** — เนื้อหาทั้งหมดเป็น HTML คงที่ JS ทำหน้าที่เพียง:

1. เตรียมข้อมูลใน state (bookings, queue, matches)
2. โหลด user session (ถ้ามี)
3. ตรวจจับหน้าปัจจุบัน
4. ตั้งค่า nav toggle สำหรับมือถือ

ข้อมูลที่โหลดในหน้า main จะถูกใช้ต่อเมื่อผู้ใช้ไปหน้าอื่น (booking, matchmaking, user, admin) เพราะทุกหน้าใช้ `app.js` ไฟล์เดียวกันและอ่านจาก `state` เดียวกัน
